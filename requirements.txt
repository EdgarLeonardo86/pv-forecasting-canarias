# Requirements consolidado para el DESPLIEGUE del dashboard en
# Streamlit Community Cloud (que solo instala desde un único
# requirements.txt en la raíz del repo). Cada agente sigue teniendo
# su propio requirements.txt para uso local independiente; este
# fichero cubre justo lo que dashboard/app.py necesita en tiempo de
# ejecución (Prophet y pvlib no hacen falta aquí, porque el dashboard
# no los usa directamente).

pandas>=2.0
numpy>=1.24
pyarrow>=14.0
streamlit>=1.30
plotly>=5.18
xgboost>=2.0
lightgbm>=4.0
scikit-learn>=1.3
