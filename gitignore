# Entornos virtuales
venv/
.venv/
env/
ENV/

# Datos generados (se regeneran ejecutando los agentes)
# Se excluye solo el histórico crudo (pesado, regenerable con el
# Agente 1). features/models/alertas SÍ se suben, porque el dashboard
# desplegado en Streamlit Cloud los necesita para funcionar.
data/raw/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# Editores / IDEs
.vscode/
.idea/
*.swp

# Logs
*.log

# Variables de entorno / credenciales (por si se añaden Telegram/email reales más adelante)
.env
.env.*
secrets.json
